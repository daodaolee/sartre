# Closeout: Real Handoff Execution And Failure Audit

## Summary

Completed the real handoff execution and failure-audit slice.

- Connector now writes failed Codex execution facts back to Hub after `delivery.running`.
- Failed execution records `model_run.status=failed`, safe `error.category/message`, and handoff/delivery/provider metadata.
- Connector calls the public delivery failure API so Hub persists `delivery.failed`.
- Web Console task detail now shows failed model-run category and safe message in the existing execution facts panel.
- `connector handoff-smoke <dev|qa> --real-codex` runs a full public Hub lifecycle smoke.
- Smoke handoff prompt is bounded so it verifies lifecycle execution instead of inviting repo exploration.

## Key Runtime Evidence

- First handoff smoke with `SARTRE_CODEX_TIMEOUT_MS=60000`: `SKIPPED`, `Timeout`, `codex timed out after 60000ms`.
- Hub writeback for that failure:
  - delivery `delivery_9fecd8f1-53df-4391-8e69-850069d532a5` ended `failed`.
  - model run `model_run_da62b999-a54d-491e-8491-340caa782b1a` recorded `status=failed`, `category=Timeout`.
  - event replay included `delivery.accepted`, `delivery.running`, and `delivery.failed`.
- Second unbounded smoke with `SARTRE_CODEX_TIMEOUT_MS=180000`: `SKIPPED`, `Timeout`, and Hub also recorded failed model run/delivery facts.
- Minimal real Codex executor smoke succeeded:
  - `SARTRE_CODEX_TIMEOUT_MS=60000 pnpm --filter @sartre/connector-cli start -- codex-smoke --exec`
  - result `REAL_TEST`, detail `SARTRE_CODEX_SMOKE_OK`.
- Bounded real handoff smoke succeeded:
  - `SARTRE_CODEX_TIMEOUT_MS=180000 pnpm --filter @sartre/connector-cli start -- handoff-smoke qa --real-codex`
  - result `REAL_TEST`.
  - delivery `delivery_25d4db59-4dd0-4b61-a28f-3fe9c9e65037` reached `report_ready`.
  - model run `model_run_efd8bc66-02c5-4dd2-bbc9-1f692f07a9cc` recorded `status=succeeded`.
  - event replay included `delivery.accepted`, `delivery.running`, `artifact.report_returned`, and `delivery.report_ready`.

## Verification Evidence

- `pnpm exec openspec validate real-handoff-execution-failure-audit --type change --strict --no-interactive`: PASS.
- `pnpm run architecture:check`: PASS.
- `git diff --check`: PASS.
- `pnpm --filter @sartre/connector-core test`: PASS, 24 tests.
- `pnpm --filter @sartre/connector-core build`: PASS.
- `pnpm --filter @sartre/connector-cli test`: PASS, 17 tests.
- `pnpm --filter @sartre/connector-cli build`: PASS.
- `pnpm --filter @sartre/web-console test`: PASS, 29 tests.
- `pnpm --filter @sartre/web-console build`: PASS.

## Files Changed

- `packages/connector-core/src/index.ts`
- `packages/connector-core/src/index.test.ts`
- `apps/connector-cli/src/index.ts`
- `apps/connector-cli/src/index.test.ts`
- `apps/web-console/src/App.tsx`
- `apps/web-console/src/App.test.tsx`
- `apps/web-console/src/styles.css`
- `openspec/changes/real-handoff-execution-failure-audit/*`
- `bdd/features/real-handoff-execution-failure-audit.md`
- `acceptance/checklists/real-handoff-execution-failure-audit.md`
- `reports/real-handoff-execution-failure-audit/*`

## Boundary

- No generic chat composer was added.
- No first-class `computer` product object was added.
- No Hub implementation internals were imported by Connector or Web Console.
- No database-breaking migration was required.
- Existing successful execution path remains public-SDK based.

## Remaining Risk

- Real Codex runtime latency is environment dependent. The bounded smoke is now suitable for lifecycle verification, but open-ended role tasks can still require longer budgets.
- The worktree remains broadly dirty from previous goals; this closeout only covers `real-handoff-execution-failure-audit`.

## Recovery Notes

To recover this goal, start from:

- `openspec/changes/real-handoff-execution-failure-audit/tasks.md`
- `reports/real-handoff-execution-failure-audit/regression/latest.md`
- Hub facts for successful smoke delivery `delivery_25d4db59-4dd0-4b61-a28f-3fe9c9e65037`
- Hub facts for failed smoke delivery `delivery_9fecd8f1-53df-4391-8e69-850069d532a5`

