# Checkpoint: 02 Web Console Agent Reconnect Control Closeout

- Time: 20260623T104758Z
- change-name: web-console-agent-reconnect-control
- VCS revision: 9b09ab2
- Scope: expose endpoint reconnect in Web Console operations panel.

## Changed Files

- `apps/web-console/src/App.tsx`
- `apps/web-console/src/App.test.tsx`
- `scripts/harness-regression.sh`
- `openspec/changes/web-console-agent-reconnect-control/**`
- `bdd/features/web-console-agent-reconnect-control.md`
- `acceptance/checklists/web-console-agent-reconnect-control.md`
- `plan/web-console-agent-reconnect-control-goal.md`
- `reports/web-console-agent-reconnect-control/**`

## Key Diff Summary

- Added `Connect endpoint` button to the replay operation block.
- The button calls `operations.connectActor(selectedActor, currentCursor)`.
- Existing `runOperation` updates the selected actor cursor from `nextCursor`.
- Added App test proving QA reconnect result appears and later replay uses the returned cursor.
- Added scoped and Lane A harness coverage for the new change.

## Commands

- `pnpm exec openspec validate web-console-agent-reconnect-control --type change --strict --no-interactive`
- `pnpm --filter @sartre/web-console test`
- `pnpm --filter @sartre/web-console build`
- `pnpm run lint:lane-a`
- `pnpm run architecture:check`
- `CHANGE_NAME=web-console-agent-reconnect-control pnpm harness:regression`
- `CHANGE_NAME=lane-a-service-baseline pnpm harness:regression`
- `pnpm harness:evidence -- --change lane-a-service-baseline`
- `git diff --check`

## Test Results

- REAL_TEST: Web Console tests passed, 2 files / 20 tests.
- REAL_TEST: Web Console build passed.
- REAL_TEST: `pnpm run lint:lane-a` passed.
- REAL_TEST: `pnpm run architecture:check` passed.
- REAL_TEST/STRUCTURAL_CHECK: scoped regression passed at `reports/web-console-agent-reconnect-control/regression/20260623T104649Z-regression-report.md`.
- REAL_TEST/STRUCTURAL_CHECK: Lane A regression passed at `reports/lane-a-service-baseline/regression/20260623T104902Z-regression-report.md`.
- STRUCTURAL_CHECK: evidence gate passed with 27 checks / 17 REAL_TEST.
- REAL_TEST: `git diff --check` passed.

## Skipped Items

- None for this scoped goal.

## Key Decisions

- Reconnect belongs in the existing operations panel for the first version.
- Full setup wizard and health diagnostics remain a separate next goal.
- No service changes were needed because `connectActor` and Hub redelivery were already validated by the real smoke.

## Output Constraints

- No Hub API route changes.
- No database migration.
- No package boundary relaxation.

## Risks and Follow-Up

- The current control is operation-level; it does not yet provide a full health-check wizard.
- Next goal should define endpoint diagnostics fields and wire setup wizard state to real service data.

## Recovery Notes

- If this regresses, inspect `apps/web-console/src/App.tsx` `OperationsPanel` and `apps/web-console/src/App.test.tsx`.
- The UI behavior contract is: `Connect endpoint` uses selected actor + current cursor, logs result, and advances cursor from `nextCursor`.
