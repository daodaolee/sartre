# Closeout: Connector Health Probe Submission

## Summary

Implemented Connector-side endpoint health probing and submission:

- `packages/connector-core` now builds generic endpoint health reports with executor, workspace, inbox, artifact, and trial-run checks.
- `packages/connector-core` submits health reports through `reportEndpointHealth(endpointId, request)`.
- `apps/connector-cli` now exposes `connector health <dev|qa>` through a testable `runConnectorCli` assembly root.
- Lane A regression now includes Connector CLI tests/build and the new `connector-health-probe-submission` change.

## Boundary Confirmed

- No Hub API route, database migration, or contract schema change was introduced.
- No real Codex, Claude, MCP, plugin, hook, command, or subagent execution was added.
- Health reports remain provider/role neutral and do not require repo, branch, commit range, or Dev-to-QA payload fields.
- CLI health submission does not acknowledge or process a handoff.

## Verification Evidence

- `pnpm --filter @sartre/connector-core test`: passed 6 tests.
- `pnpm --filter @sartre/connector-cli test`: passed 5 tests.
- `pnpm --filter @sartre/connector-core build`: passed.
- `pnpm --filter @sartre/connector-cli build`: passed.
- `pnpm run lint:lane-a`: passed.
- `pnpm run architecture:check`: passed.
- `CHANGE_NAME=connector-health-probe-submission pnpm harness:regression`: passed.
- `CHANGE_NAME=lane-a-service-baseline pnpm harness:regression`: passed.
- `pnpm harness:evidence -- --change lane-a-service-baseline`: passed with 41 checks / 25 REAL_TEST.
- `pnpm exec openspec validate connector-health-probe-submission --type change --strict --no-interactive`: passed.
- `git diff --check`: passed.

## Reports

- Scoped regression: `reports/connector-health-probe-submission/regression/20260623T115110Z-regression-report.md`
- Lane A regression: `reports/lane-a-service-baseline/regression/20260623T115132Z-regression-report.md`

## Post-Goal Plan Calibration

Previous plan remains valid, with one adjustment: because Connector can now submit health, the next highest-value goal is a Web Console health refresh/interpretation slice. It should keep using Hub overview and avoid importing Connector or Hub internals.
