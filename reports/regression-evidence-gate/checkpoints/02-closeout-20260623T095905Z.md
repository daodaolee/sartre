# Regression Evidence Gate Closeout

## Result

PASS. Lane A regression now validates its generated latest report through `harness:evidence`.

## What Changed

- Added `scripts/evidence-gate.ts`.
- Added `scripts/evidence-gate.test.ts`.
- Added root script: `pnpm harness:evidence`.
- Updated `scripts/harness-regression.sh`:
  - `record_skip` now writes `Command: n/a`.
  - `finish_report` runs `pnpm harness:evidence -- --change lane-a-service-baseline` after writing `latest.md`.
  - Lane A regression fails if the evidence gate fails.
- Updated `lint:lane-a` so the evidence gate scripts are included in lane lint.

## Verification

- `pnpm exec vitest run scripts/evidence-gate.test.ts`: PASS, 4 tests.
- `pnpm harness:evidence -- --change lane-a-service-baseline`: PASS, 22 checks / 14 REAL_TEST.
- `pnpm run lint:lane-a`: PASS.
- `CHANGE_NAME=lane-a-service-baseline pnpm harness:regression`: PASS.
- `pnpm exec openspec validate regression-evidence-gate --type change --strict --no-interactive`: PASS.
- `git diff --check`: PASS.

Latest Lane A regression report:

- `reports/lane-a-service-baseline/regression/20260623T095905Z-regression-report.md`

## Notes

- This gate validates report trustworthiness; it does not replace underlying business tests.
- No domain, service, Web, desktop, database, or connector behavior changed.
- No commit, push, publish, archive, reset, or revert was performed.
