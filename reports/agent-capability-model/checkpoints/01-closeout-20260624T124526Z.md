# Agent Capability Model Closeout

Timestamp: `20260624T124526Z`

## Scope

Implemented the first durable Agent capability model across shared contracts, SDK, Hub API persistence, Local Connector profiles, and Web Console projection.

This change keeps task payloads role-neutral. Agent endpoints now explain their own readable capability sources, executor binding, and approval policy.

## Implemented

- Added `CapabilitySource`, `ExecutorBinding`, and `ApprovalPolicy` contract schemas.
- Preserved `capabilities: string[]` for matching and backward compatibility.
- Added additive Postgres JSONB fields for `capability_sources`, `executor`, and `approval_policy`.
- Updated Hub repository and overview projection to persist and return endpoint manifests.
- Updated Connector profiles, registration payloads, inbox markdown, and health reporting for capability sources.
- Updated Web Console Agents, Hooks, and Skills views to prefer Hub overview manifest data and only use demo fallback when no manifest exists.

## Runtime Smoke Evidence

- Confirmed port `3000` Hub API process cwd:
  `/Users/xy/personal/Sartre(agent-workspace-design)/.worktrees/lane-a-service-baseline/apps/hub-api`
- Registered smoke endpoint through HTTP:
  `POST /agent-endpoints` -> `201 Created`
- Verified Postgres row in `sartre_hub`:
  `smoke_capability_codex_local`, `source_count=2`, `executor_kind=codex_cli`, `approval_mode=manual_confirm`
- Verified `GET /overview?tenant_id=local-demo` projection through Node `fetch`:
  `status=200`, `source_count=2`, `executor_kind=codex_cli`, `approval_mode=manual_confirm`

## Verification

- `pnpm --filter @sartre/contracts test` -> 1 file, 14 tests passed
- `pnpm --filter @sartre/sdk test` -> 1 file, 11 tests passed
- `pnpm --filter @sartre/connector-core test` -> 1 file, 11 tests passed
- `pnpm --filter @sartre/web-console test` -> 4 files, 26 tests passed
- `DATABASE_URL=postgresql://xy@localhost:55432/sartre_hub_verify_capability_final pnpm --filter @sartre/hub-api test` -> 4 files, 14 tests passed
- `pnpm --filter @sartre/web-console build` -> passed
- `pnpm --filter @sartre/hub-api build` -> passed
- `pnpm --filter @sartre/contracts build` -> passed
- `pnpm --filter @sartre/sdk build` -> passed
- `pnpm --filter @sartre/connector-core build` -> passed
- `pnpm run architecture:check` -> passed
- `pnpm exec openspec validate agent-capability-model --type change --strict --no-interactive` -> passed
- `git diff --check` -> passed

## Remaining Limitations

- `codex_cli` is only a declared executor binding. This change does not run Codex CLI, control Codex desktop, or execute local commands from Hub.
- Approval policy is persisted and projected, but automatic execution policy enforcement is still a follow-up.
- Platform chat runtime is not implemented. The next design should use a Sartre-owned, model-neutral conversation ledger so switching between Codex, Claude, or other LLMs can rebuild context from platform facts.
- The chat-runtime follow-up can reference `/Users/xy/xykj/superagentai` for session messages, memory recall, summary refresh, provider/model administration, and projection separation, but should avoid binding Sartre continuity to any provider-specific session id.

## Next Goal Candidates

- `platform-chat-runtime`: conversation ledger, message persistence, tool invocation records, summary checkpoints, model runs, provider-specific context projection, and streaming UI projection.
- `local-codex-executor`: local connector adapter that turns approved tasks into Codex CLI/manual prompt work while preserving human approval gates.
- `approval-policy-enforcement`: settings-driven manual confirm versus auto read-only versus low-risk execution gates, with audit events for every transition.
