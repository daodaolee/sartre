# Monorepo Package Isolation Governance Closeout

## Result

PASS. Lane A service baseline no longer treats Connector CLI as a Hub API build dependency.

## Boundary Changes

- Added public shared package: `packages/connector-core`.
- Kept local demo profile facts in `@sartre/contracts`.
- Updated `apps/connector-cli` to be a CLI shell over `@sartre/connector-core`.
- Updated `apps/hub-api/src/scripts/local-connector-demo.ts` to import connector behavior from `@sartre/connector-core`, not `@sartre/connector-cli`.
- Updated `apps/hub-api/package.json` to depend on `@sartre/connector-core`, not `@sartre/connector-cli`.
- Strengthened `scripts/architecture-check.ts` and `scripts/architecture-check.test.ts` to reject app-to-app source imports and app-to-app package dependencies.
- Updated `lint:lane-a` and `CHANGE_NAME=lane-a-service-baseline pnpm harness:regression` to verify service baseline packages plus `@sartre/connector-core`, without Connector CLI as a Lane A build gate.

## Verification

- `pnpm exec vitest run scripts/architecture-check.test.ts`: PASS, 2 tests.
- `pnpm --filter @sartre/contracts test`: PASS, 7 tests.
- `pnpm --filter @sartre/connector-core test`: PASS, 3 tests.
- `pnpm --filter @sartre/connector-cli test`: PASS, 3 tests.
- `pnpm --filter @sartre/hub-api test`: PASS, 4 files / 9 tests.
- `pnpm --filter @sartre/contracts build`: PASS.
- `pnpm --filter @sartre/connector-core build`: PASS.
- `pnpm --filter @sartre/connector-cli build`: PASS.
- `pnpm --filter @sartre/hub-api build`: PASS.
- `pnpm run lint:lane-a`: PASS.
- `pnpm run architecture:check`: PASS.
- `CHANGE_NAME=lane-a-service-baseline pnpm harness:regression`: PASS.

Regression report:

- `reports/lane-a-service-baseline/regression/20260623T093047Z-regression-report.md`

## Notes

- No Web Console source changes were required in this worktree.
- No database schema changes were made.
- No commit, push, publish, archive, reset, or revert was performed.
