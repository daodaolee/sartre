# Checkpoint: 02 Final Closeout

- 时间：2026-06-23T06:26:58Z
- change-name：event-store-and-delivery-state
- VCS revision：jj `nutvrrluxonzrputryqtwqvwqonpkrml` / git `315c9528d076`
- 范围：Handoff Hub delivery lifecycle event store、failed/expired terminal state、overview timeline/metrics、SDK/Web coverage、scoped harness。
- 变更文件：
  - `packages/domain/src/handoff/delivery.ts`
  - `packages/domain/src/handoff/delivery.test.ts`
  - `packages/contracts/src/index.ts`
  - `packages/contracts/src/handoff-contracts.test.ts`
  - `apps/hub-api/src/modules/handoff/**`
  - `packages/sdk/src/index.ts`
  - `packages/sdk/src/index.test.ts`
  - `apps/web-console/src/App.test.tsx`
  - `scripts/harness-regression.sh`
  - `openspec/changes/event-store-and-delivery-state/tasks.md`
  - `acceptance/checklists/event-store-and-delivery-state.md`
  - `reports/event-store-and-delivery-state/checkpoints/PLAN_LEDGER.md`
- 关键 diff 摘要：
  - Delivery 聚合新增 `failed` / `expired` 终态、`fail()` / `expire()` 显式转换和 restore 入口。
  - Contracts 扩展 delivery status，并新增 terminal command / lifecycle event schema。
  - Hub Postgres 新增 `delivery_events` 表与 `failed_at` / `expired_at` 当前状态字段。
  - Repository 在 create、reconnect、ack、fail、expire、report artifact 路径中事务化写状态和事件。
  - Overview timeline 改读 `delivery_events`，`failed_deliveries` 改按真实 delivery status 计数。
  - Hub API 新增 `POST /deliveries/:deliveryId/fail` 与 `POST /deliveries/:deliveryId/expire`。
  - SDK 新增 `failDelivery()` 与 `expireDelivery()`。
  - Web Console 增加 failed/expired overview fixture 渲染覆盖；现有泛化渲染无需 production UI 改动。
  - Scoped harness 新增 `event-store-and-delivery-state` 分支。
- 执行命令：
  - `pnpm exec openspec validate event-store-and-delivery-state --type change --strict --no-interactive`
  - `pnpm --filter @sartre/domain test`
  - `pnpm --filter @sartre/contracts test`
  - `pnpm --filter @sartre/sdk test`
  - `pnpm --filter @sartre/web-console test`
  - `pnpm --filter @sartre/hub-api test`
  - `pnpm --filter @sartre/hub-api build`
  - `pnpm --filter @sartre/sdk build`
  - `pnpm --filter @sartre/web-console build`
  - `pnpm run lint:v0.2`
  - `pnpm run architecture:check`
  - `CHANGE_NAME=event-store-and-delivery-state pnpm harness:regression`
- 测试结果：
  - `REAL_TEST`：Domain tests PASS，Contracts tests PASS，Hub API E2E PASS，SDK tests PASS，Web Console tests PASS。
  - `REAL_TEST`：Hub API build PASS，SDK build PASS，Web Console build PASS。
  - `REAL_TEST`：`lint:v0.2` PASS，`architecture:check` PASS。
  - `STRUCTURAL_CHECK`：OpenSpec strict validate PASS，BDD/acceptance/ledger 文件存在。
  - `REAL_TEST`：Scoped harness PASS，报告 `reports/event-store-and-delivery-state/regression/20260623T062918Z-regression-report.md`。
- 跳过项和原因：
  - Full repo historical lint/build/diff gate：`STRUCTURAL_CHECK` skip，当前 goal 使用 scoped gate；历史工作区已有大量非本 goal 改动。
- 关键决策：
  - 不做完整 event sourcing；`deliveries` 保留当前状态，`delivery_events` 作为 append-only audit/read-model fact source。
  - Event payload 保持 metadata-only，不引入 dev-to-QA 固定字段。
  - Web Console 不 import Hub API 内部实现，只通过 contracts/SDK 消费 overview。
- 输出约束：
  - No Electron/Tauri runtime changes。
  - No auth/scheduler/pagination/tenant UI。
  - No fixed business payload schema for role-specific handoffs.
- 风险和待办：
  - 旧历史数据没有事件行，不在本 change 回填范围。
  - 当前 schema 仍由 `ensureSchema()` 管理；部署化前应迁移到正式 migration。
  - `POST /deliveries/:id/ack` 对终态非法转换仍由 Nest 默认映射为 500；若要产品化错误码，可单独做 HTTP error mapping goal。
- 恢复说明：
  - 若 failed/expired 状态行为异常，从 `packages/domain/src/handoff/delivery.ts` 和 `packages/domain/src/handoff/delivery.test.ts` 恢复检查。
  - 若 overview timeline/metrics 异常，从 `apps/hub-api/src/modules/handoff/infrastructure/postgres/postgres-handoff.repository.ts` 的 `delivery_events` 查询和 `toOverviewTimelineEvent()` 恢复检查。
  - 若客户端消费异常，从 `packages/contracts/src/index.ts`、`packages/sdk/src/index.ts`、`apps/web-console/src/App.test.tsx` 恢复检查。
