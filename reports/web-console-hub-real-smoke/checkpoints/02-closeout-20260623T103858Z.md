# Checkpoint: 02 Web Console Hub Real Smoke Closeout

- Time: 20260623T103858Z
- change-name: web-console-hub-real-smoke
- VCS revision: 9b09ab2
- Scope: add a real integration smoke proving Web Console operations can drive a local Hub API through public SDK/contracts.

## Changed Files

- `scripts/web-console-hub-real-smoke.test.ts`
- `apps/web-console/src/hub-operations.ts`
- `apps/web-console/src/hub-operations.test.ts`
- `apps/web-console/src/App.test.tsx`
- `package.json`
- `pnpm-lock.yaml`
- `scripts/harness-regression.sh`
- `openspec/changes/web-console-hub-real-smoke/**`
- `bdd/features/web-console-hub-real-smoke.md`
- `acceptance/checklists/web-console-hub-real-smoke.md`
- `plan/web-console-hub-real-smoke-goal.md`
- `reports/web-console-hub-real-smoke/**`

## Key Diff Summary

- Added root `web:smoke:hub` script.
- Added root workspace dev dependency on `@sartre/sdk` for script-level integration harnesses.
- Added `connectActor(actor, lastSeenCursor)` to Web Console operations.
- Added a real smoke that starts Nest `AppModule`, drives actions through `HandoffHubClient` and Web operations, and asserts endpoint, delivery, replay, ack, and timeline state.
- Added this change to scoped and Lane A regression.

## Commands

- `pnpm exec openspec validate web-console-hub-real-smoke --type change --strict --no-interactive`
- `pnpm exec vitest run scripts/web-console-hub-real-smoke.test.ts`
- `pnpm --filter @sartre/web-console test`
- `pnpm --filter @sartre/web-console build`
- `pnpm run lint:lane-a`
- `pnpm run architecture:check`
- `CHANGE_NAME=web-console-hub-real-smoke pnpm harness:regression`
- `CHANGE_NAME=lane-a-service-baseline pnpm harness:regression`
- `pnpm harness:evidence -- --change lane-a-service-baseline`
- `git diff --check`

## Test Results

- REAL_TEST: `pnpm run web:smoke:hub` passed.
- REAL_TEST: `pnpm --filter @sartre/web-console test` passed, 2 files / 19 tests.
- REAL_TEST: `pnpm --filter @sartre/web-console build` passed.
- REAL_TEST: `pnpm run lint:lane-a` passed.
- REAL_TEST: `pnpm run architecture:check` passed.
- REAL_TEST/STRUCTURAL_CHECK: scoped regression passed at `reports/web-console-hub-real-smoke/regression/20260623T103709Z-regression-report.md`.
- REAL_TEST/STRUCTURAL_CHECK: Lane A regression passed at `reports/lane-a-service-baseline/regression/20260623T104022Z-regression-report.md`.
- STRUCTURAL_CHECK: evidence gate passed with 26 checks / 17 REAL_TEST.
- REAL_TEST: `git diff --check` passed.

## Skipped Items

- None for this scoped goal.

## Key Decisions

- Keep the real smoke under `scripts/` so it can import the Hub app for harness bootstrapping without making `apps/web-console` depend on Hub internals.
- Add reconnect as a Web operation because offline QA pending delivery cannot be acknowledged until the endpoint reconnects and receives redelivery.
- Do not add a Web UI reconnect button in this goal; keep the slice to operation and harness evidence.

## Output Constraints

- No Hub API route changes.
- No database migration.
- No Electron/Tauri packaging changes.
- No external credentials.

## Risks and Follow-Up

- The smoke resets the shared local Hub test database through `SARTRE_TEST_RESET=true`; keep it sequential in harness.
- Next goal should wire Agent setup/health-check UX to real service data and decide where reconnect appears in the UI.

## Recovery Notes

- If this slice regresses, start from `scripts/web-console-hub-real-smoke.test.ts` and `apps/web-console/src/hub-operations.ts`.
- The critical contract is `connectActor -> HandoffHubClient.connectAgentEndpoint -> overview refresh`.
- The aggregate evidence is `reports/lane-a-service-baseline/regression/20260623T104022Z-regression-report.md`.
