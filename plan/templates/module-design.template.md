# 模块设计模板

> 复制本模板到 `plan/modules/Mxx-<name>.md` 填写。每个模块的 plan 必须覆盖以下全部小节。AI 按此 plan 实施，实施中的偏离记入"变更记录"。

---

## DoD 速查卡

```
本模块完成 = 功能全 ✓ + 契约合规 ✓ + SOLID/架构/状态机合规 ✓
           + 测试达标 ✓ + 审计报告(带证据) ✓ + 看板更新 ✓ + 集成通过 ✓
```

## 1. 模块概述

- **ID / 名称**：Mxx / xxx
- **一句话职责**：
- **docs 出处**：docs/xx
- **里程碑归属**：MSx
- **复杂度**：低/中/高

## 2. 职责清单（实现完整性的验收依据）

- [ ] 职责 1
- [ ] 职责 2
- ...（每条对应可验证的功能点）

**明确不负责**（边界，防 SRP 越界）：
- 不做 xxx（由 Myy 负责）

## 3. 依赖

| 依赖模块 | 依赖的契约 | 用途 |
|---------|-----------|------|
| Myy | `XxxTrait` | ... |

被谁依赖：

## 4. 对外契约（按 05 规范，五要素）

### 4.1 接口（trait）
```rust
// trait 定义
```

### 4.2 数据结构
```rust
// 公开输入输出结构
```

### 4.3 错误类型（映射到 05 统一语义）
| 错误 | 语义类别 | 调用方应对 |
|------|---------|-----------|

### 4.4 行为保证
| 方法 | 前置 | 后置 | 不变量 | 幂等 |
|------|------|------|-------|------|

### 4.5 能力声明（如适用）
```rust
// capabilities 结构
```

## 5. 内部设计

- 关键数据结构：
- 关键算法/流程：
- 可插拔点（trait + 注入方式）：
- 用到的设计模式及理由：

## 6. 状态机（如适用，按 03 规范）

### 状态枚举
```rust
```

### 转换表（与代码分支一一对应）
| 当前状态 | 事件 | 新状态 | 副作用 |
|---------|------|-------|-------|

## 7. 测试计划（按 04，测试先行）

| 测试类型 | 用例 | 验证点 |
|---------|------|-------|
| 单元-正常 | | |
| 单元-边界 | | |
| 单元-错误 | | |
| 单元-状态机 | | |
| 集成 | | |
| 降级/并发（如适用） | | |

- 覆盖率目标（按模块类型）：行 ≥ __% / 分支 ≥ __%
- Mock 策略：

## 8. 实施步骤（垂直、可增量验证）

1. 写契约 trait + 数据结构
2. 写测试（红）
3. 实现到绿
4. ...

## 9. 风险与未决问题

| 风险/问题 | 影响 | 缓解/待决 |
|----------|------|----------|

## 10. 变更记录（实施中偏离留痕，见 07）

| 日期 | 变更 | 原因 | 影响 | 决策者 |
|------|------|------|------|-------|

## 11. 审计链接

- 最新审计报告：plan/audits/Mxx-<日期>.md
- 状态：⬜/🟨/🟦/✅/🟥
